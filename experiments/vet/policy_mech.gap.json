{
  "source": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
  "emitted_items": [
    "PolicySite"
  ],
  "gaps": [
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 33,
      "col": 1,
      "category": "Import",
      "rust_construct": "Import",
      "snippet": "use mycelium_core :: ContentHash ;",
      "reason": "`use mycelium_core::ContentHash` — `mycelium_core` is not a sibling module OR sibling phylum transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2)",
      "item_name": null
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 34,
      "col": 1,
      "category": "Import",
      "rust_construct": "Import",
      "snippet": "use mycelium_select :: { select , Candidate , Explanation , PolicyRegistry , SelectError , SelectionInputs , SelectionPolicy , } ;",
      "reason": "`use mycelium_select::select` — `mycelium_select` is not a sibling module OR sibling phylum transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use mycelium_select::Candidate` — `mycelium_select` is not a sibling module OR sibling phylum transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use mycelium_select::Explanation` — `mycelium_select` is not a sibling module OR sibling phylum transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use mycelium_select::PolicyRegistry` — `mycelium_select` is not a sibling module OR sibling phylum transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use mycelium_select::SelectError` — `mycelium_select` is not a sibling module OR sibling phylum transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use mycelium_select::SelectionInputs` — `mycelium_select` is not a sibling module OR sibling phylum transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2); `use mycelium_select::SelectionPolicy` — `mycelium_select` is not a sibling module OR sibling phylum transpiled in this same batch (an external crate, `std`, or simply out of this batch's target set). Flagged, not guessed (VR-5/G2)",
      "item_name": null
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 38,
      "col": 1,
      "category": "DeriveSatisfied",
      "rust_construct": "DeriveSatisfied",
      "snippet": "# [doc = \" The RFC-0005 policy sites (§4: swap-target, packing; RFC-0008 RT3 adds placement as the\"] # [doc = \" third). A [`PolicySlot`] is keyed by site so a set/select is always attributed to the site\"] # [doc = \" it governs (provenance, RFC-0001 §4.3).\"] # [derive (Debug , Clone , Copy , PartialEq , Eq)] pub enum PolicySite { # [doc = \" The RFC-0002 swap-target site.\"] SwapTarget , # [doc = \" The RFC-0004 §5 packing site.\"] Packing , # [doc = \" The RFC-0008 RT3 placement site (single-node in Phase I — DN-78 §3; the multi-node\"] # [doc = \" candidate set is deferred, see [`crate::r2_residual`]).\"] Placement , }",
      "reason": "enum `PolicySite` derive(Clone) is a satisfied no-op under Mycelium's value semantics (ADR-003 — every value already copies structurally; DN-128 §6.1) — not emitted as an impl, not a gap",
      "item_name": "PolicySite"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 38,
      "col": 1,
      "category": "DeriveSatisfied",
      "rust_construct": "DeriveSatisfied",
      "snippet": "# [doc = \" The RFC-0005 policy sites (§4: swap-target, packing; RFC-0008 RT3 adds placement as the\"] # [doc = \" third). A [`PolicySlot`] is keyed by site so a set/select is always attributed to the site\"] # [doc = \" it governs (provenance, RFC-0001 §4.3).\"] # [derive (Debug , Clone , Copy , PartialEq , Eq)] pub enum PolicySite { # [doc = \" The RFC-0002 swap-target site.\"] SwapTarget , # [doc = \" The RFC-0004 §5 packing site.\"] Packing , # [doc = \" The RFC-0008 RT3 placement site (single-node in Phase I — DN-78 §3; the multi-node\"] # [doc = \" candidate set is deferred, see [`crate::r2_residual`]).\"] Placement , }",
      "reason": "enum `PolicySite` derive(Copy) is a satisfied no-op under Mycelium's value semantics (ADR-003 — every value already copies structurally; DN-128 §6.1) — not emitted as an impl, not a gap",
      "item_name": "PolicySite"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 38,
      "col": 1,
      "category": "DeriveSatisfied",
      "rust_construct": "DeriveSatisfied",
      "snippet": "# [doc = \" The RFC-0005 policy sites (§4: swap-target, packing; RFC-0008 RT3 adds placement as the\"] # [doc = \" third). A [`PolicySlot`] is keyed by site so a set/select is always attributed to the site\"] # [doc = \" it governs (provenance, RFC-0001 §4.3).\"] # [derive (Debug , Clone , Copy , PartialEq , Eq)] pub enum PolicySite { # [doc = \" The RFC-0002 swap-target site.\"] SwapTarget , # [doc = \" The RFC-0004 §5 packing site.\"] Packing , # [doc = \" The RFC-0008 RT3 placement site (single-node in Phase I — DN-78 §3; the multi-node\"] # [doc = \" candidate set is deferred, see [`crate::r2_residual`]).\"] Placement , }",
      "reason": "enum `PolicySite` derive(Eq): covered by co-occurring PartialEq → `fn eq_PolicySite` (product-row collision policy; DN-128 / ONESHOT C2) — not a second emission",
      "item_name": "PolicySite"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 52,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl core :: fmt :: Display for PolicySite { fn fmt (& self , f : & mut core :: fmt :: Formatter < '_ >) -> core :: fmt :: Result { match self { PolicySite :: SwapTarget => write ! (f , \"swap-target\") , PolicySite :: Packing => write ! (f , \"packing\") , PolicySite :: Placement => write ! (f , \"placement\") , } } }",
      "reason": "trait-impl of non-prelude trait `Display` — no ambient trait definition and synthetic trait emission is refused (DN-34 §8.8 / DN-122 MVP); gapped the whole impl so the residual cannot file-poison myc-check (G2; express gap-close 2026-07-16)",
      "item_name": "PolicySite"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 62,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" A reified policy-set transition record (G2: a mechanized set is never a silent override —\"] # [doc = \" the transition itself is inspectable). Guarantee: **`Exact`** — every [`PolicySlot::set`]\"] # [doc = \" appends exactly one record, with a per-slot monotonic sequence number, by construction.\"] # [derive (Debug , Clone , PartialEq , Eq)] pub struct PolicySetRecord { # [doc = \" The site whose active policy changed.\"] pub site : PolicySite , # [doc = \" Per-slot monotonic sequence number (0 for the first set).\"] pub seq : u64 , # [doc = \" The previous active policy's content address, `None` on the first set.\"] pub previous : Option < ContentHash > , # [doc = \" The new active policy's content address ([`SelectionPolicy::policy_ref`]).\"] pub new_policy : ContentHash , # [doc = \" The new policy's display name (for the EXPLAIN/teaching surface).\"] pub new_policy_name : String , }",
      "reason": "struct `PolicySetRecord` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "PolicySetRecord"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 79,
      "col": 1,
      "category": "PayloadVariant",
      "rust_construct": "PayloadVariant",
      "snippet": "# [doc = \" Why a slot operation failed — always explicit (G2), never a silent default choice.\"] # [derive (Debug , Clone , PartialEq)] pub enum SlotError { # [doc = \" Selection was requested but no policy has been set for this site. Guarantee: **`Exact`**\"] # [doc = \" — fail-closed by construction; there is no built-in fallback policy (a silent default\"] # [doc = \" would be a black box, ADR-006).\"] NoActivePolicy { # [doc = \" The site that has no active policy.\"] site : PolicySite , } , # [doc = \" The underlying RFC-0005 selection refused (e.g. an out-of-range override).\"] Select (SelectError) , }",
      "reason": "enum `SlotError` has a named-field variant referencing a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "SlotError"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 93,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl core :: fmt :: Display for SlotError { fn fmt (& self , f : & mut core :: fmt :: Formatter < '_ >) -> core :: fmt :: Result { match self { SlotError :: NoActivePolicy { site } => { write ! (f , \"no active policy is set for the {site} site — set one explicitly \\\n                     (PolicySlot::set); there is no silent default (G2/ADR-006)\") } SlotError :: Select (e) => write ! (f , \"selection refused: {e}\") , } } }",
      "reason": "trait-impl of non-prelude trait `Display` — no ambient trait definition and synthetic trait emission is refused (DN-34 §8.8 / DN-122 MVP); gapped the whole impl so the residual cannot file-poison myc-check (G2; express gap-close 2026-07-16)",
      "item_name": "SlotError"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 108,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl std :: error :: Error for SlotError { }",
      "reason": "trait-impl of non-prelude trait `Error` — no ambient trait definition and synthetic trait emission is refused (DN-34 §8.8 / DN-122 MVP); gapped the whole impl so the residual cannot file-poison myc-check (G2; express gap-close 2026-07-16)",
      "item_name": "SlotError"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 110,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl From < SelectError > for SlotError { fn from (e : SelectError) -> Self { SlotError :: Select (e) } }",
      "reason": "trait-impl of non-prelude trait `From` — no ambient trait definition and synthetic trait emission is refused (DN-34 §8.8 / DN-122 MVP); gapped the whole impl so the residual cannot file-poison myc-check (G2; express gap-close 2026-07-16)",
      "item_name": "SlotError"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 116,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" A runtime slot binding the **active** [`SelectionPolicy`] for one RFC-0005 site, with an\"] # [doc = \" append-only transition log and a selection trace (DN-78 §3 B-2).\"] # [doc = \"\"] # [doc = \" The slot is the mechanized *setter surface*: `set` swaps the active policy and records the\"] # [doc = \" transition; `select` decides through the active policy and records the mandatory\"] # [doc = \" [`Explanation`]. Both logs are read-only views (`transitions`/`trace`) — append-only by\"] # [doc = \" construction (no public mutation besides the appending operations).\"] # [derive (Debug)] pub struct PolicySlot { site : PolicySite , active : Option < SelectionPolicy > , transitions : Vec < PolicySetRecord > , trace : Vec < Explanation > , }",
      "reason": "struct `PolicySlot` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "PolicySlot"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 131,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl PolicySlot { # [doc = \" An empty slot for `site` — no active policy, no transitions, no trace.\"] # [must_use] pub fn new (site : PolicySite) -> Self { PolicySlot { site , active : None , transitions : Vec :: new () , trace : Vec :: new () , } } # [doc = \" The site this slot governs.\"] # [must_use] pub fn site (& self) -> PolicySite { self . site } # [doc = \" Set the active policy, appending a [`PolicySetRecord`] (returned by reference).\"] # [doc = \"\"] # [doc = \" Guarantee: **`Exact`** — exactly one record is appended per call, `seq` is the per-slot\"] # [doc = \" monotonic count, and `previous` is the outgoing policy's content address (`None` on the\"] # [doc = \" first set). The transition is never silent (G2).\"] pub fn set (& mut self , policy : SelectionPolicy) -> & PolicySetRecord { let record = PolicySetRecord { site : self . site , seq : self . transitions . len () as u64 , previous : self . active . as_ref () . map (SelectionPolicy :: policy_ref) , new_policy : policy . policy_ref () , new_policy_name : policy . name () . to_owned () , } ; self . active = Some (policy) ; self . transitions . push (record) ; self . transitions . last () . expect (\"push in the line above guarantees a last element\") } # [doc = \" The active policy, if one has been set. `None` is not a fallback state — selection\"] # [doc = \" through an unset slot refuses explicitly ([`SlotError::NoActivePolicy`]).\"] # [must_use] pub fn active (& self) -> Option < & SelectionPolicy > { self . active . as_ref () } # [doc = \" The append-only transition log (every `set`, in order).\"] # [must_use] pub fn transitions (& self) -> & [PolicySetRecord] { & self . transitions } # [doc = \" The selection trace: the mandatory [`Explanation`] of every selection made through this\"] # [doc = \" slot, in order — extractable for capture/diffing (DN-78 §3 B-2).\"] # [must_use] pub fn trace (& self) -> & [Explanation] { & self . trace } # [doc = \" Decide through the active policy (RFC-0005 `select`), recording the mandatory\"] # [doc = \" [`Explanation`] into the slot's trace.\"] # [doc = \"\"] # [doc = \" Errors are explicit: an unset slot is [`SlotError::NoActivePolicy`] (never a silent\"] # [doc = \" default — G2/ADR-006); an underlying selection refusal passes through as\"] # [doc = \" [`SlotError::Select`].\"] pub fn select (& mut self , inputs : & SelectionInputs , forced : Option < usize > ,) -> Result < (Candidate , Explanation) , SlotError > { let site = self . site ; let policy = self . active . as_ref () . ok_or (SlotError :: NoActivePolicy { site }) ? ; let (candidate , explanation) = select (policy , inputs , forced) ? ; self . trace . push (explanation . clone ()) ; Ok ((candidate , explanation)) } }",
      "reason": "no member of this impl block could be transpiled (7 sub-issue(s)): impl method `new` body: struct literal `PolicySlot { .. }` — not an in-file single-ctor struct or enum struct-variant that emits (no constructor to build; a cross-nodule variant is an honest DN-113/DN-134-OQ-2 resolvability gap, never a fabricated out-of-file reference — VR-5/G2); impl method `site` body: field projection `self.site` on `PolicySlot` — not an in-file single-ctor struct that emits (an enum / external / non-resolvable type has no constructor to `match` against); impl method `set` body: struct literal `PolicySetRecord { .. }` — not an in-file single-ctor struct or enum struct-variant that emits (no constructor to build; a cross-nodule variant is an honest DN-113/DN-134-OQ-2 resolvability gap, never a fabricated out-of-file reference — VR-5/G2); impl method `active` body: Rust ownership/identity-conversion no-op method `.as_ref()` has no Mycelium free-function/prim referent (value semantics — ADR-003); desugaring it to a bare `as_ref(recv)` would fabricate an unknown prim (`unknown function/constructor/prim `as_ref`` — verified against the oracle), so it is gapped, never fake-emitted (G2/VR-5); impl method `transitions` body: field projection `self.transitions` on `PolicySlot` — not an in-file single-ctor struct that emits (an enum / external / non-resolvable type has no constructor to `match` against); impl method `trace` body: field projection `self.trace` on `PolicySlot` — not an in-file single-ctor struct that emits (an enum / external / non-resolvable type has no constructor to `match` against); impl method `select` body: field projection `self.site` on `PolicySlot` — not an in-file single-ctor struct that emits (an enum / external / non-resolvable type has no constructor to `match` against)",
      "item_name": "PolicySlot"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 211,
      "col": 1,
      "category": "Struct",
      "rust_construct": "Struct",
      "snippet": "# [doc = \" A captured policy: the RFC-0005-conformant [`SelectionPolicy`] value that decided a recorded\"] # [doc = \" [`Explanation`], materialized for reuse/diffing (DN-78 §3 B-1). Not an opaque handle — the\"] # [doc = \" full policy value is inspectable (ADR-006).\"] # [derive (Debug , Clone , PartialEq)] pub struct CapturedPolicy { # [doc = \" The policy's content address (equal to `policy.policy_ref()` — verified at capture).\"] pub policy_ref : ContentHash , # [doc = \" The policy value itself.\"] pub policy : SelectionPolicy , }",
      "reason": "struct `CapturedPolicy` uses named fields and references a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "CapturedPolicy"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 222,
      "col": 1,
      "category": "PayloadVariant",
      "rust_construct": "PayloadVariant",
      "snippet": "# [doc = \" Why a capture failed — always explicit (G2), never a silent reconstruction.\"] # [derive (Debug , Clone , PartialEq , Eq)] pub enum CaptureError { # [doc = \" The recorded `policy_ref` resolves to nothing in the given registry. Guarantee:\"] # [doc = \" **`Exact`** — fail-closed by construction; capture never fabricates a policy.\"] UnknownPolicyRef { # [doc = \" The unresolvable content address.\"] policy_ref : ContentHash , } , # [doc = \" The registry returned a policy whose own content address differs from the requested one\"] # [doc = \" (a corrupted registry). Never silently accepted.\"] RefMismatch { # [doc = \" The content address the capture asked for.\"] requested : ContentHash , # [doc = \" The content address the resolved policy actually hashes to.\"] resolved : ContentHash , } , }",
      "reason": "enum `CaptureError` has a named-field variant referencing a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "CaptureError"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 241,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl core :: fmt :: Display for CaptureError { fn fmt (& self , f : & mut core :: fmt :: Formatter < '_ >) -> core :: fmt :: Result { match self { CaptureError :: UnknownPolicyRef { policy_ref } => { write ! (f , \"policy_ref {policy_ref:?} is not in the registry — capture refuses rather \\\n                     than reconstructing a policy (G2/ADR-006)\") } CaptureError :: RefMismatch { requested , resolved , } => { write ! (f , \"registry corruption: requested {requested:?} but the stored policy hashes \\\n                     to {resolved:?}\") } } } }",
      "reason": "trait-impl of non-prelude trait `Display` — no ambient trait definition and synthetic trait emission is refused (DN-34 §8.8 / DN-122 MVP); gapped the whole impl so the residual cannot file-poison myc-check (G2; express gap-close 2026-07-16)",
      "item_name": "CaptureError"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 265,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl std :: error :: Error for CaptureError { }",
      "reason": "trait-impl of non-prelude trait `Error` — no ambient trait definition and synthetic trait emission is refused (DN-34 §8.8 / DN-122 MVP); gapped the whole impl so the residual cannot file-poison myc-check (G2; express gap-close 2026-07-16)",
      "item_name": "CaptureError"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 267,
      "col": 1,
      "category": "Other",
      "rust_construct": "Other",
      "snippet": "# [doc = \" Materialize the policy that decided `explanation` from `registry` (DN-78 §3 B-1).\"] # [doc = \"\"] # [doc = \" Guarantee: **`Exact`** for the resolution contract — an unknown ref is an explicit\"] # [doc = \" [`CaptureError::UnknownPolicyRef`] and a hash mismatch an explicit\"] # [doc = \" [`CaptureError::RefMismatch`]; a returned [`CapturedPolicy`] always satisfies\"] # [doc = \" `policy.policy_ref() == policy_ref` (checked here, not assumed).\"] pub fn capture (registry : & PolicyRegistry , explanation : & Explanation ,) -> Result < CapturedPolicy , CaptureError > { let requested = explanation . policy . clone () ; let policy = registry . get (& requested) . ok_or_else (| | CaptureError :: UnknownPolicyRef { policy_ref : requested . clone () , }) ? ; let resolved = policy . policy_ref () ; if resolved != requested { return Err (CaptureError :: RefMismatch { requested , resolved , }) ; } Ok (CapturedPolicy { policy_ref : requested , policy : policy . clone () , }) }",
      "reason": "Rust ownership/identity-conversion no-op method `.clone()` has no Mycelium free-function/prim referent (value semantics — ADR-003); desugaring it to a bare `clone(recv)` would fabricate an unknown prim (`unknown function/constructor/prim `clone`` — verified against the oracle), so it is gapped, never fake-emitted (G2/VR-5)",
      "item_name": "capture"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 296,
      "col": 1,
      "category": "PayloadVariant",
      "rust_construct": "PayloadVariant",
      "snippet": "# [doc = \" Why a replay failed — always explicit (G2), never a silent pass.\"] # [derive (Debug , Clone , PartialEq)] pub enum ReplayError { # [doc = \" The captured policy is not the one the record claims decided it (`policy_ref`\"] # [doc = \" mismatch) — replaying against the wrong policy would be a silent apples-to-oranges\"] # [doc = \" comparison, so it refuses up front.\"] PolicyMismatch { # [doc = \" The record's policy content address.\"] recorded : ContentHash , # [doc = \" The captured policy's content address.\"] captured : ContentHash , } , # [doc = \" Re-running the recorded inputs refused (e.g. a recorded override index that no longer\"] # [doc = \" fits the policy — impossible for a faithful capture, but never silently swallowed).\"] Select (SelectError) , # [doc = \" The replayed decision differs from the recorded one. With a validated policy and\"] # [doc = \" identical inputs this indicates non-determinism or a record from different code —\"] # [doc = \" either way it is surfaced, never absorbed.\"] Diverged { # [doc = \" The original record.\"] recorded : Box < Explanation > , # [doc = \" The replayed record that differs from it.\"] replayed : Box < Explanation > , } , }",
      "reason": "enum `ReplayError` has a named-field variant referencing a type not resolvable in-file — emitting it positionally would introduce an unresolved reference that poisons the file's `myc check`; left gapped under the M-1006 resolvability gate (VR-5/G2)",
      "item_name": "ReplayError"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 322,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl core :: fmt :: Display for ReplayError { fn fmt (& self , f : & mut core :: fmt :: Formatter < '_ >) -> core :: fmt :: Result { match self { ReplayError :: PolicyMismatch { recorded , captured } => write ! (f , \"replay refused: the record was decided by {recorded:?} but the captured \\\n                 policy is {captured:?}\") , ReplayError :: Select (e) => write ! (f , \"replay selection refused: {e}\") , ReplayError :: Diverged { recorded , replayed } => write ! (f , \"replay diverged: recorded chose index {} (rule {:?}), replay chose index {} \\\n                 (rule {:?})\" , recorded . chosen_index , recorded . matched_rule , replayed . chosen_index , replayed . matched_rule) , } } }",
      "reason": "trait-impl of non-prelude trait `Display` — no ambient trait definition and synthetic trait emission is refused (DN-34 §8.8 / DN-122 MVP); gapped the whole impl so the residual cannot file-poison myc-check (G2; express gap-close 2026-07-16)",
      "item_name": "ReplayError"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 344,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl std :: error :: Error for ReplayError { }",
      "reason": "trait-impl of non-prelude trait `Error` — no ambient trait definition and synthetic trait emission is refused (DN-34 §8.8 / DN-122 MVP); gapped the whole impl so the residual cannot file-poison myc-check (G2; express gap-close 2026-07-16)",
      "item_name": "ReplayError"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 346,
      "col": 1,
      "category": "Impl",
      "rust_construct": "Impl",
      "snippet": "impl From < SelectError > for ReplayError { fn from (e : SelectError) -> Self { ReplayError :: Select (e) } }",
      "reason": "trait-impl of non-prelude trait `From` — no ambient trait definition and synthetic trait emission is refused (DN-34 §8.8 / DN-122 MVP); gapped the whole impl so the residual cannot file-poison myc-check (G2; express gap-close 2026-07-16)",
      "item_name": "ReplayError"
    },
    {
      "file": "/root/git/isolated/mycelium/crates/mycelium-std-runtime/src/policy_mech.rs",
      "line": 352,
      "col": 1,
      "category": "MultiStmtBody",
      "rust_construct": "MultiStmtBody",
      "snippet": "# [doc = \" Replay a recorded decision against its captured policy (DN-78 §3 B-1): re-run the recorded\"] # [doc = \" inputs — honoring the recorded override state — and require the identical [`Explanation`].\"] # [doc = \"\"] # [doc = \" Guarantee: **`Empirical`** for \\\"replay reaches the recorded decision\\\" — the record-vs-replay\"] # [doc = \" differential is property-tested over randomized policies/inputs; RFC-0005 `select` is\"] # [doc = \" deterministic (same `(policy, inputs, forced)` → same result) but that determinism carries\"] # [doc = \" no mechanized theorem, so the claim is not `Proven` (VR-5; M-964 audit). A divergence is an\"] # [doc = \" explicit [`ReplayError::Diverged`] carrying both records for inspection (G2).\"] pub fn replay (captured : & CapturedPolicy , recorded : & Explanation ,) -> Result < Explanation , ReplayError > { if captured . policy_ref != recorded . policy { return Err (ReplayError :: PolicyMismatch { recorded : recorded . policy . clone () , captured : captured . policy_ref . clone () , }) ; } let forced = recorded . overridden . then_some (recorded . chosen_index) ; let (_ , replayed) = select (& captured . policy , & recorded . inputs , forced) ? ; if replayed != * recorded { return Err (ReplayError :: Diverged { recorded : Box :: new (recorded . clone ()) , replayed : Box :: new (replayed) , }) ; } Ok (replayed) }",
      "reason": "function body has a semicolon-terminated (value-discarding) statement expression before the tail — a `let`-chain body maps only simple `let x = e;` bindings plus a single trailing expression",
      "item_name": "replay"
    }
  ],
  "total_top_level_items": 21
}